{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dev",
  "description": "Debug-only capabilities (E2E testing via tauri-pilot). Installed by build.rs only when the `e2e` Cargo feature is enabled.",
  "windows": ["main", "popover"],
  "permissions": ["pilot:default"]
}
